export enum UserRole {
    Admin = "Admin",
    Teacher = "Lehrperson",
    Student = "Studierend",
}

export enum QuizStatus {
    Pending = 1,
    Active = 2,
    Completed = 3,
}

export enum QuizType {
    Singleplayer = 1,
    Coop = 2,
    Versus = 3,
}
